<?xml version="1.0" encoding="utf-8"?>
<!--
  Scoped cleartext exception for local development only — the backend runs
  as plain http:// on localhost. Only 10.0.2.2 (the emulator's host-loopback
  alias) and localhost are allowed; this is not a blanket cleartext allow.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
